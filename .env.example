# Gemini API Configuration
# Get your API key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Cloud Run Proxy Configuration (optional)
# Set PROXY_MODE to one of: replay, record, live
PROXY_MODE=live

# GCS Store Configuration (optional, required for replay/record modes)
# Your Google Cloud Storage bucket name for decision store
DECISION_STORE_GCS_BUCKET=your_bucket_name_here

# GCS Store prefix (optional, defaults to "records")
DECISION_STORE_PREFIX=records

# Vite Engine Configuration (optional)
# Set to "live" for live execution or "replay" for replay mode
VITE_ENGINE_MODE=live

# Proxy URL (optional, for local development)
# Set to your proxy URL if using a separate proxy service
VITE_PROXY_URL=http://localhost:8080
